# pnpm configuration

# Automatically install peer dependencies
auto-install-peers=true

# Use strict peer dependencies (warn but don't fail)
strict-peer-dependencies=false

# Hoist node_modules for better compatibility with Next.js
node-linker=hoisted

# Improve reproducibility
save-exact=true

shamefully-hoist=true
engine-strict=true